// Crie uma função que verifica se um número é par
function verifica(){
    const x = + prompt('Digite um número')
    if(x%2==0){
        alert(`${x} é par`)
    } else {
        alert(`${x} é impar`)
    }
}

